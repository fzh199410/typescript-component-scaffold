import * as React from 'react';
import './index.css';
import {useState} from "react";
const styles = require('./index.css.json');

const Uploader = () => {
    return (
        <div className={styles['mro-react-uploader']}>
            Uploader
        </div>
    );
};

export default Uploader;
